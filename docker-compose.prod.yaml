---

version: '3.8'

services:
  ldap-notifier:
    image: artifacts.knut.univention.de/upx/ldap/notifier:${CI_PIPELINE_TAG}test
  ldap-server:
    image: artifacts.knut.univention.de/upx/ldap/server:${CI_PIPELINE_TAG}test

...
